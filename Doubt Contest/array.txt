https://practice.geeksforgeeks.org/problems/kth-smallest-element/0


The following code works but it is exceeding time limit which is 1.636 seconds.
i am out of options to make it work within 1.636 seconds for any input. any help will be appreciated. :)


#include<bits/stdc++.h>
#include<set>
using namespace std;

int main() 
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int t,k;
	cin>>t;
	for(int x=1;x<=t;x++)
	{
	    int n,s;
	    cin>>n;
	    set<int> A;
	    for(int i=0;i<n;i++)
	    {
	        cin>>k;
	        A.insert(k);
	    }
	    cin>>s;
	    set<int>::iterator it=A.begin();
	   for(int i=1;i<s;i++)
	      it++;
	   cout<<*it<<'\n';
	    
	}
}
